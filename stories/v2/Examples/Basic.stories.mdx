import { Meta, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../../src/v2";

<Meta
  title="V2 (neeto-atoms)/Examples/Basic"
  parameters={{
    layout: "padded",
    previewTabs: { canvas: { hidden: true } },
  }}
  component={Editor}
/>

# Basic — v2

Renders the editor from the v2 entry. Behaves the same as v1 today; visual
polish ships in Wave 6.

<Canvas>
  <Editor autoFocus />
</Canvas>
