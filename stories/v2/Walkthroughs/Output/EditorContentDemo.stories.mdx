import { Meta, Unstyled } from "@storybook/addon-docs";

import EditorContentDemo from "./EditorContentDemo";

<Meta
  title="V2 (neeto-atoms)/Walkthroughs/Output/EditorContent demo"
  parameters={{
    layout: "padded",
    viewMode: "docs",
    previewTabs: { canvas: { hidden: true } },
  }}
/>

# EditorContent demo — v2

To update the preview content, make use of the editor below. The changes will
be instantly reflected in the v2 `EditorContent` preview above.

<Unstyled>
  <EditorContentDemo />
</Unstyled>
