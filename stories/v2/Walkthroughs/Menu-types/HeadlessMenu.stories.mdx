import { Meta, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../../../src/v2";

<Meta
  title="V2 (neeto-atoms)/Walkthroughs/Menu types/Headless menu"
  parameters={{
    layout: "padded",
    previewTabs: { canvas: { hidden: true } },
  }}
  component={Editor}
/>

# Headless menu — v2

The `headless` menu lets you customize the menu in its raw form. This story
exercises the migrated v2 `Headless/Option` and the `Emoji`, `Color`, and
`MediaUploader` headless surfaces.

<Canvas>
  <Editor
    autoFocus
    menuType="headless"
    addons={[
      "highlight",
      "emoji",
      "code-block",
      "block-quote",
      "image-upload",
      "video-upload",
    ]}
  />
</Canvas>
